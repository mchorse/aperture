# GUI

aperture.gui:
    # General strings
    done: Done
    next: Next
    previous: Previous
    restore: Restore Default
    reset: Reset
    add: Add
    edit: Edit
    remove: Remove
    detach: Detach
    record: Record
    pick: Pick morph...
    duplicate: Duplicate
    search: "Search:"
    cancel: Cancel
    break: Break
    select: Select
    ok: Ok

    config:
        title: Camera Options
        
        minema: Minema
        spectator: Spectator
        show_path: Show path
        sync: Sync (S)
        flight: Flight mode (F)

    tooltips:
        jump_next_fixture: Jump to next fixture (Shift + Right)
        jump_next_frame: Jump to next frame (Right)
        plause: Play/pause (Space)
        jump_prev_frame: Jump to previous frame (Left)
        jump_prev_fixture: Jump to previous fixture (Shift + Left)

        move_up: Move fixture forward
        move_duration: Move duration to the cursor (M)
        copy_position: Copy player's position (B)
        move_down: Move fixture backward

        save: Save camera profile
        profiles: Show camera profiles
        config: Show camera editor options
        add: Add fixture
        remove: Remove selected fixture

    editor:
        f1: Press F1 to show GUI again...

    fixtures:
        idle: Idle
        path: Path
        look: Look
        follow: Follow
        circular: Circular

    profiles:
        loaded: Loaded
        load: Load
        new: New
        tooltip: New camera name

    panels:
        duration: Duration

        x: X
        y: "Y"
        z: Z

        yaw: Yaw
        pitch: Pitch
        roll: Roll
        fov: FOV

        offset: Offset
        circles: Circles
        distance: Distance

        path_points: Path points
        position: Position
        angle: Angle
        circle: Circle
        selector: Selector
        per_point: Per point duration

        interps:
            linear: Linear
            cubic: Cubic
            hermite: Hermite
